# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

target 'TappableTest' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TappableTest
  pod 'Alamofire', '~> 5.2'

  target 'TappableTestTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TappableTestUITests' do
    # Pods for testing
  end

end
